---
/**
 * Shown on every page carrying affiliate links. Required before any
 * commission link appears, per the marketing rules in CLAUDE.md.
 */
---
<p class="affiliate-notice">
  Algunos enlaces son de afiliado: si reservas a través de ellos no pagas más y
  este proyecto se mantiene. <a href="/afiliacion/">Cómo funciona</a>.
</p>

<style>
  .affiliate-notice {
    font-size: var(--step--1);
    color: var(--color-text-muted);
    background: var(--color-surface);
    padding: var(--space-sm) var(--space-md);
    border-radius: var(--radius);
    margin-block: var(--space-md);
  }
</style>
